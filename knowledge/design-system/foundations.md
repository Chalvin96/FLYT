# Design foundations

Use semantic tokens for color, spacing, typography, radius, and elevation so theme changes remain coherent. Shared ownership lives in `@flyt/ui`; app and extension surfaces consume it rather than recreating primitives.

Design for responsive layouts, focus visibility, keyboard operation, contrast, reduced motion, and clear loading/error/empty states. A component's visual choices should express learner status and remain stable across light/dark themes.
